pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'test jenkins file ! triggered from webhook'
            }
        }
        // stage('Build') {
        //     steps {
        //         sh 'mvn clean install'
        //     }
        // }
        //
    }
}
