pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'test jenkins file !'
            }
        }
        // stage('Build') {
        //     steps {
        //         sh 'mvn clean install'
        //     }
        // }
    }
}
